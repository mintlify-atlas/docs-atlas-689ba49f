---
title: httpz
mode: custom
---

<div className="relative overflow-hidden bg-gradient-to-br from-[#0f1117] via-[#1a1d27] to-[#0f1117] dark:bg-gradient-to-br dark:from-[#0f1117] dark:via-[#1a1d27] dark:to-[#0f1117] py-20 lg:py-24">
  <div className="absolute inset-0 bg-[url('/grid.svg')] opacity-10"></div>
  <div className="relative max-w-7xl mx-auto px-6 lg:px-8">
    <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-center">
      <div className="lg:col-span-7">
        <h1 className="text-4xl sm:text-5xl lg:text-6xl font-bold text-white dark:text-white mb-6">
          Zero-allocation HTTP/1.1 for OxCaml
        </h1>
        <p className="text-base sm:text-lg lg:text-xl text-gray-300 dark:text-gray-300 max-w-2xl mb-8">
          High-performance HTTP parser achieving true zero heap allocations using OxCaml's unboxed types and stack allocation. Parse at 6.5M requests/sec with RFC 7230 security compliance.
        </p>
        <div className="flex flex-wrap gap-4">
          <a 
            href="/quickstart"
            className="inline-flex items-center px-6 py-3 rounded-lg bg-[#241c24] hover:bg-[#154188] text-white dark:text-white font-semibold transition-colors no-underline"
          >
            Get Started
            <svg className="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 7l5 5m0 0l-5 5m5-5H6" />
            </svg>
          </a>
          <a 
            href="/api/httpz"
            className="inline-flex items-center px-6 py-3 rounded-lg border border-white/30 bg-white/10 hover:bg-white/20 text-white dark:text-white font-semibold transition-colors no-underline"
          >
            API Reference
          </a>
        </div>
      </div>
      <div className="lg:col-span-5 hidden lg:block">
        <div className="rounded-2xl bg-[#1a1d27] dark:bg-[#1a1d27] border border-[#27272a] dark:border-[#27272a] p-6 shadow-2xl">
          <div className="flex items-center gap-2 mb-4">
            <div className="w-3 h-3 rounded-full bg-red-500"></div>
            <div className="w-3 h-3 rounded-full bg-yellow-500"></div>
            <div className="w-3 h-3 rounded-full bg-green-500"></div>
          </div>
          <pre className="text-sm text-gray-300 dark:text-gray-300 overflow-x-auto"><code>{`let buf = Httpz.create_buffer () in
let len = read_from_socket buf in
let #(status, req, headers) = 
  Httpz.parse buf ~len 
    ~limits:Httpz.default_limits in
match status with
| Buf_read.Complete ->
  (* Zero allocations! *)
  let method = req.#meth in
  let target = req.#target in
  ...`}</code></pre>
        </div>
      </div>
    </div>
  </div>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <h2 className="text-2xl sm:text-3xl font-semibold text-gray-900 dark:text-white mb-3">
    Quick start
  </h2>
  <p className="text-base text-gray-600 dark:text-gray-400 mb-8">
    Get up and running with httpz in minutes
  </p>
  
  <Steps>
    <Step title="Install OxCaml compiler">
      httpz requires the OxCaml compiler with support for unboxed types. Install from [oxcaml.org](https://oxcaml.org/).
      
      ```bash
      # Verify you have OCaml 5.2+ with OxCaml support
      ocaml --version
      ```
    </Step>
    
    <Step title="Add httpz to your project">
      Install httpz via opam:
      
      ```bash
      opam install httpz
      ```
      
      Add to your dune file:
      
      ```lisp
      (executable
       (name my_server)
       (libraries httpz async))
      ```
    </Step>
    
    <Step title="Parse your first request">
      Create a simple parser that achieves zero allocations:
      
      ```ocaml
      open Httpz
      
      let parse_request socket_data =
        let buf = Httpz.create_buffer () in
        (* Read data into buffer *)
        let len = read_socket_into buf socket_data in
        
        (* Parse with zero heap allocations *)
        let #(status, req, headers) = 
          Httpz.parse buf ~len ~limits:Httpz.default_limits 
        in
        
        match status with
        | Buf_read.Complete ->
          (* All values are stack-allocated *)
          Printf.printf "Method: %s\n" (Method.to_string req.#meth);
          Printf.printf "Target: %s\n" (Span.to_string buf req.#target);
          List.iter (fun hdr ->
            Printf.printf "Header: %s\n" 
              (Header_name.canonical hdr.Header.name)
          ) headers
        | _ -> 
          Printf.eprintf "Parse failed\n"
      ```
      
      <Note>
        The `#(...)` syntax creates an unboxed tuple, and `req.#field` accesses unboxed record fields. Everything stays on the stack!
      </Note>
    </Step>
    
    <Step title="Try the static file server">
      httpz includes a production-ready static file server:
      
      ```bash
      # Serve current directory on port 8080
      dune exec bin/httpz_server.exe
      
      # Custom directory and port
      dune exec bin/httpz_server.exe -- -d /var/www -p 3000
      ```
      
      The server handles 10,000 concurrent connections with zero-copy I/O and automatic MIME type detection.
    </Step>
  </Steps>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <h2 className="text-2xl sm:text-3xl font-semibold text-gray-900 dark:text-white mb-3">
    Key features
  </h2>
  <p className="text-base text-gray-600 dark:text-gray-400 mb-8">
    Built for performance with OxCaml's advanced type system
  </p>
  
  <CardGroup cols={2}>
    <Card title="True zero allocations" icon="bolt" href="/concepts/zero-allocation">
      Parser results are entirely stack-allocated using unboxed records and local lists. No GC pressure.
    </Card>
    <Card title="Unboxed types throughout" icon="microchip" href="/concepts/unboxed-types">
      Uses int16#, int64#, and char# for offsets and lengths. No boxing overhead.
    </Card>
    <Card title="RFC 7230 security" icon="shield-halved" href="/guides/security">
      Prevents request smuggling with bare CR detection and ambiguous framing checks.
    </Card>
    <Card title="Complete HTTP/1.1 support" icon="globe" href="/guides/parsing-requests">
      Chunked encoding, ETags, range requests, keep-alive, and conditional requests.
    </Card>
    <Card title="6.5M requests/sec" icon="gauge-high" href="/concepts/architecture">
      Benchmarked at 154ns for minimal requests with 45x fewer allocations than alternatives.
    </Card>
    <Card title="Direct bigstring I/O" icon="memory" href="/api/buf-read">
      Read and write directly to bigarrays for zero-copy integration with Async.
    </Card>
  </CardGroup>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <h2 className="text-2xl sm:text-3xl font-semibold text-gray-900 dark:text-white mb-3">
    Explore the documentation
  </h2>
  <p className="text-base text-gray-600 dark:text-gray-400 mb-8">
    Deep dive into httpz's architecture and API
  </p>
  
  <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
    <a href="/concepts/architecture" className="group block rounded-2xl border border-gray-200 dark:border-[#27272a] hover:border-[#241c24] dark:hover:border-[#241c24] overflow-hidden transition-colors no-underline">
      <div className="p-6 bg-gray-50 dark:bg-[#1a1d27]">
        <h3 className="text-base font-semibold text-gray-900 dark:text-white mb-2">Core Concepts</h3>
        <p className="text-sm text-gray-600 dark:text-gray-400 mb-3">
          Learn how httpz achieves zero allocations with unboxed records, local lists, and span-based parsing.
        </p>
        <div className="flex items-center text-sm text-gray-600 dark:text-gray-400 group-hover:text-[#241c24] dark:group-hover:text-[#241c24] transition-colors">
          Read more
          <svg className="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
          </svg>
        </div>
      </div>
    </a>
    
    <a href="/guides/parsing-requests" className="group block rounded-2xl border border-gray-200 dark:border-[#27272a] hover:border-[#241c24] dark:hover:border-[#241c24] overflow-hidden transition-colors no-underline">
      <div className="p-6 bg-gray-50 dark:bg-[#1a1d27]">
        <h3 className="text-base font-semibold text-gray-900 dark:text-white mb-2">Guides</h3>
        <p className="text-sm text-gray-600 dark:text-gray-400 mb-3">
          Step-by-step guides for parsing requests, writing responses, handling chunked encoding, ETags, and range requests.
        </p>
        <div className="flex items-center text-sm text-gray-600 dark:text-gray-400 group-hover:text-[#241c24] dark:group-hover:text-[#241c24] transition-colors">
          Browse guides
          <svg className="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
          </svg>
        </div>
      </div>
    </a>
    
    <a href="/api/httpz" className="group block rounded-2xl border border-gray-200 dark:border-[#27272a] hover:border-[#241c24] dark:hover:border-[#241c24] overflow-hidden transition-colors no-underline">
      <div className="p-6 bg-gray-50 dark:bg-[#1a1d27]">
        <h3 className="text-base font-semibold text-gray-900 dark:text-white mb-2">API Reference</h3>
        <p className="text-sm text-gray-600 dark:text-gray-400 mb-3">
          Complete API documentation for all modules: Httpz, Parser, Request, Response, and HTTP feature modules.
        </p>
        <div className="flex items-center text-sm text-gray-600 dark:text-gray-400 group-hover:text-[#241c24] dark:group-hover:text-[#241c24] transition-colors">
          View API docs
          <svg className="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
          </svg>
        </div>
      </div>
    </a>
    
    <a href="/server/overview" className="group block rounded-2xl border border-gray-200 dark:border-[#27272a] hover:border-[#241c24] dark:hover:border-[#241c24] overflow-hidden transition-colors no-underline">
      <div className="p-6 bg-gray-50 dark:bg-[#1a1d27]">
        <h3 className="text-base font-semibold text-gray-900 dark:text-white mb-2">Static File Server</h3>
        <p className="text-sm text-gray-600 dark:text-gray-400 mb-3">
          Production-ready Async server with zero-copy I/O, MIME detection, and support for 10,000 concurrent connections.
        </p>
        <div className="flex items-center text-sm text-gray-600 dark:text-gray-400 group-hover:text-[#241c24] dark:group-hover:text-[#241c24] transition-colors">
          Learn more
          <svg className="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
          </svg>
        </div>
      </div>
    </a>
  </div>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <div className="rounded-2xl bg-gradient-to-br from-[#241c24] to-[#154188] p-8 lg:p-12 text-center">
    <h2 className="text-2xl sm:text-3xl font-semibold text-white dark:text-white mb-4">
      Ready to build high-performance HTTP servers?
    </h2>
    <p className="text-base text-gray-200 dark:text-gray-200 max-w-2xl mx-auto mb-8">
      Start using httpz to parse HTTP/1.1 requests with zero allocations and maximum throughput.
    </p>
    <a 
      href="/quickstart"
      className="inline-flex items-center px-6 py-3 rounded-lg bg-white hover:bg-gray-100 text-[#241c24] font-semibold transition-colors no-underline"
    >
      Get Started Now
      <svg className="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 7l5 5m0 0l-5 5m5-5H6" />
      </svg>
    </a>
  </div>
</div>
